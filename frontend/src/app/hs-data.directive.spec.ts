import { HsDataDirective } from './hs-data.directive';

describe('HsDataDirective', () => {
  it('should create an instance', () => {
    const directive = new HsDataDirective();
    expect(directive).toBeTruthy();
  });
});
